server:
  port: 51608
spring:
  application:
    name: huahuo-ffpo-shop
  cloud:
    nacos:
      discovery:
        server-addr: fancyday.fit:8849
      config:
        server-addr: fancyday.fit:8849
        file-extension: yml
        namespace: huahuo
        ephemeral: false
feign:
  hystrix:
    enabled: true