server:
  port: 51806
spring:
  application:
    name: huahuo-ffpo-user
  cloud:
    nacos:
      discovery:
        server-addr: fancyday.fit:8849
      config:
        server-addr: fancyday.fit:8849
        file-extension: yml
        namespace: huahuo
        ephemeral: false
logging:
  level=debug:
feign:
  hystrix:
    enabled: true