server:
  port: 51601
spring:
  application:
    name: ffpo-gateway
  cloud:
    nacos:
      discovery:
        server-addr: fancyday.fit:8849
      config:
        server-addr: fancyday.fit:8849
        file-extension: yml
        namespace: huahuo